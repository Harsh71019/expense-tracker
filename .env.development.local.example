# Overrides for a host-native `pnpm dev` process. Docker Compose continues to use .env.
REDIS_URL=redis://localhost:6379
DATABASE_URL=postgres://treasury_ops:local-dev-password@localhost:5433/treasury_ops
