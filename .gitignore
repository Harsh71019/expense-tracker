node_modules/
dist/
.next/
.env
.env.*.local
infra/**/.env
coverage/
*.tsbuildinfo
playwright-report/
test-results/
.DS_Store
.design-sync
.ds-sync
ds-bundle

# server-side pre-deploy backup tooling (deliberately untracked -- see
# backup.sh's own header comment for why)
backup.sh
restore.sh
backups/
*.dump