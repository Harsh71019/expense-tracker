import type { ReactNode } from "react";

import { ComingSoon } from "@/components/ui";

export default function RecurringPage(): ReactNode {
  return <ComingSoon title="Recurring transactions" phase="Planned" />;
}
